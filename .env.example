# Nexos.ai — OpenAI-compatible LLM gateway (server only)
NEXOS_API_KEY="ide-jon-a-nexos-kulcs"
NEXOS_BASE_URL="https://api.nexos.ai/v1"
NEXOS_MODEL="gpt-4o"

# Google Gemini — image generation / virtual try-on (server only)
GEMINI_API_KEY="ide-jon-a-gemini-kulcs"
GEMINI_IMAGE_MODEL="gemini-2.5-flash-image-preview"
GEMINI_TEXT_MODEL="gemini-2.5-flash"

# Optional local database override
DATABASE_URL="file:./dev.db"
